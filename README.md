- [F1 进站预测 — Kaggle Playground S6E5](#f1-进站预测--kaggle-playground-s6e5)
  - [目录](#目录)
  - [快速开始](#快速开始)
  - [环境要求](#环境要求)
  - [数据集说明](#数据集说明)
  - [项目结构](#项目结构)
  - [代码架构](#代码架构)
    - [执行流程](#执行流程)
  - [特征工程](#特征工程)
    - [轮胎配方交互](#轮胎配方交互)
    - [轮胎寿命非线性变换](#轮胎寿命非线性变换)
    - [退化衍生](#退化衍生)
    - [位置与排名](#位置与排名)
    - [比赛进度与停站交叉](#比赛进度与停站交叉)
    - [时序滑动窗口](#时序滑动窗口)
    - [轮胎配方基准偏差](#轮胎配方基准偏差)
    - [分组统计与偏差](#分组统计与偏差)
    - [5 折交叉目标编码](#5-折交叉目标编码)
    - [风险乘积特征](#风险乘积特征)
    - [频率编码](#频率编码)
  - [模型配置](#模型配置)
    - [统一训练范式](#统一训练范式)
    - [6 个模型一览](#6-个模型一览)
    - [各模型详细参数](#各模型详细参数)
      - [lgb\_deep（LightGBM 深度大容量）](#lgb_deeplightgbm-深度大容量)
      - [lgb\_reg（LightGBM 正则泛化）](#lgb_reglightgbm-正则泛化)
      - [xgb\_deep（XGBoost 深度）](#xgb_deepxgboost-深度)
      - [xgb\_reg（XGBoost 浅树正则）](#xgb_regxgboost-浅树正则)
      - [cat\_gpu（CatBoost）](#cat_gpucatboost)
      - [nn\_residual（PyTorch 残差网络）](#nn_residualpytorch-残差网络)
  - [集成策略](#集成策略)
    - [模型相关性分析](#模型相关性分析)
    - [第一层：L-BFGS-B 权重优化](#第一层l-bfgs-b-权重优化)
    - [第二层：5 折交叉堆叠](#第二层5-折交叉堆叠)
    - [最终融合](#最终融合)
  - [输出文件](#输出文件)
  - [模型效果](#模型效果)
  - [常见问题](#常见问题)
    - [运行报错](#运行报错)
    - [效果调优](#效果调优)
  - [调参方向](#调参方向)
    - [全局超参](#全局超参)
    - [树模型](#树模型)
    - [神经网络](#神经网络)
    - [集成](#集成)
  - [License](#license)

# F1 进站预测 — Kaggle Playground S6E5

基于 F1 赛事圈级数据，预测车手下一圈是否进站的二分类项目。代码集成 LightGBM、XGBoost、CatBoost 和一个 PyTorch 残差网络，通过权重优化 + 二层堆叠融合输出最终结果。支持 GPU 自动加速，单文件一键跑通。

| 项目 | 说明 |
|------|------|
| 赛事 | Kaggle Playground Series S6E5 |
| 任务 | 二分类（车手下一圈是否进站） |
| 指标 | ROC-AUC |
| 版本 | V8.1 GPU 版 |
| 主文件 | `v-8.1.py` |
| 运行方式 | 单文件执行，GPU/CPU 自适应 |

---

## 目录

- [快速开始](#快速开始)
- [环境要求](#环境要求)
- [数据集说明](#数据集说明)
- [项目结构](#项目结构)
- [代码架构](#代码架构)
- [特征工程](#特征工程)
- [模型配置](#模型配置)
- [集成策略](#集成策略)
- [输出文件](#输出文件)
- [模型效果](#模型效果)
- [常见问题](#常见问题)
- [调参方向](#调参方向)

---

## 快速开始

```bash
# 1. 安装依赖
pip install pandas numpy scikit-learn lightgbm xgboost catboost torch scipy

# 2. 准备数据（从 Kaggle 下载后放入下述目录）
#    playground-series-s6e5/
#    ├── train.csv
#    ├── test.csv
#    └── sample_submission.csv

# 3. 运行
python v-8.1.py
```

运行结束后，当前目录会生成 `submission_v8_gpu.csv`，可直接上传 Kaggle 提交。无 GPU 环境会自动降级为 CPU 运行，无需改代码。

---

## 环境要求

| 项目 | 要求 |
|------|------|
| Python | 3.8+ |
| GPU（可选） | NVIDIA CUDA 11.x 及以上 |
| 显存（建议） | 6 GB 以上 |
| CPU 运行耗时 | 约 X 小时（5 折 × 5 种子 × 6 模型） |
| GPU 运行耗时 | 约 X 分钟 |

**主要依赖及建议版本**：

```
pandas, numpy
scikit-learn
lightgbm, xgboost, catboost
torch (CUDA 版本需与本地 CUDA 匹配)
scipy
```

> 注意：新版 LightGBM（4.0+）已将 `device='gpu'` 改为 `device='cuda'`，如遇 GPU 不识别请检查版本并相应调整参数。本项目代码中已做兼容处理。

---

## 数据集说明

| 项目 | 说明 |
|------|------|
| 来源 | Kaggle Playground Series S6E5 |
| 训练集 | `train.csv`，圈级数据 |
| 测试集 | `test.csv` |
| 目标列 | `PitNextLap`（0/1，下一圈是否进站） |
| 正负样本比 | 约 1:5（全局先验均值 gm = 0.1990） |
| 提交样例 | `sample_submission.csv` |

数据需放在 `playground-series-s6e5/` 目录下，与代码同级。代码启动时会打印训练集和测试集的形状，用于快速校验数据完整性。

---

## 项目结构

```
.
├── v-8.1.py                      # 主程序（单文件闭环）
├── playground-series-s6e5/       # 数据目录
│   ├── train.csv
│   ├── test.csv
│   └── sample_submission.csv
├── submission_v8_gpu.csv         # 运行后生成：最终提交文件
├── experiment_v8_gpu.json        # 运行后生成：实验记录
├── oof_*.npy                     # 运行后生成：各模型 OOF 预测（共 6 个）
└── test_*.npy                    # 运行后生成：各模型测试集预测（共 6 个）
```

---

## 代码架构

代码是一个单文件线性 Pipeline，按执行顺序自上而下分 7 个模块，模块之间单向依赖、无循环。

| 模块 | 起止位置 | 职责 |
|------|----------|------|
| 1 依赖与工具 | 文件开头～`log` 函数结束 | 导入依赖、屏蔽警告、日志工具 |
| 2 深度学习组件 | `F1Dataset`～`predict_nn` 结束 | 数据集类、残差网络、单折训练、批量预测 |
| 3 全局配置 | 全局常量～GPU 检测结束 | 超参数、设备自适应 |
| 4 数据与特征 | 数据加载～频率编码结束 | 特征衍生、分组统计、目标编码 |
| 5 特征筛选 | `FEATURES` 列表～矩阵输出 | 特征管理、去冗余、生成训练矩阵 |
| 6 多模型训练 | 多模型训练～NN 训练结束 | 5 折 × 5 种子交叉验证 |
| 7 集成输出 | 集成优化～文件结束 | 融合、堆叠、提交文件 |

### 执行流程

代码按 6 个阶段顺序执行，每阶段都有日志标记，方便定位进度：

| 阶段 | 动作 | 日志标记 |
|------|------|----------|
| 1 数据加载 | 读取训练/测试集，格式校验 | `[1/6] Loading data` |
| 2 特征工程 | 衍生 100+ 维特征 | `[2/6] Feature engineering` |
| 3 特征编码 | 5 折目标编码、频率编码 | `[3/6] KFold target encoding` |
| 4 多模型训练 | 6 模型 × 5 折 × 5 种子 | `[4/6] Multi-config training` |
| 5 集成优化 | 权重寻优 + 二层堆叠 | `[5/6] Ensemble optimization` |
| 6 结果输出 | 提交文件 + 实验记录 | `[6/6] Final submission` |

---

## 特征工程

原始数据只有十余维基础特征，经过 `add_features` 函数扩展到 100+ 维。这里按类别说明设计思路。

### 轮胎配方交互

把轮胎配方（软/中/硬）的有序编码，与轮胎寿命、圈数、比赛进度、退化、位置做乘法交叉。不同配方的衰减曲线和进站策略差别很大，交叉特征能让模型区分"同样的轮胎寿命，对不同配方意味着什么"。

### 轮胎寿命非线性变换

- 幂次：`TL_sq`、`TL_cu`
- 平滑：`TL_sqrt`、`TL_log`
- 阈值二值：`TL_gt_15`、`TL_gt_25`、`TL_gt_35`、`TL_gt_50`

轮胎寿命和进站概率不是线性关系——大约在某个拐点后概率陡升。树模型虽然能学非线性，但显式给出这些变换能降低学习难度。阈值对应不同配方的典型寿命节点。

### 退化衍生

- `Deg_per_lap`：单圈退化率
- `LTD_per_lap`：单圈圈速差
- `Deg_abs`、`Deg_sq`

单位圈的退化量比累计退化更能反映轮胎当前状态。这一组在特征重要性里排得比较靠前。

### 位置与排名

`Pos_sq`、`Is_Top10`。前十和后段车手的进站策略差异显著，二值标记用来强化这个分区。

### 比赛进度与停站交叉

`Stint_x_TL`、`PitStop_x_TL`、`Stint_x_RP`、`TL_div_RP`。同样是轮胎寿命 20 圈，比赛初期和末期含义完全不同，这组交叉刻画的是"轮胎寿命的相对价值"。

### 时序滑动窗口

按"比赛 + 车手"分组，滚动计算前 3 圈的圈速均值、退化均值、圈速变化均值，并做 1 步偏移（只用历史圈，不引入当前圈）。

> 实测中发现窗口取 3 圈效果最好。最初试过 5 圈，反而把信号稀释了——单圈圈速标准差大约是均值的 8%，窗口太长会把临近进站的突变抹平。

### 轮胎配方基准偏差

基于业务先验设定每种配方的典型寿命（软胎 12 圈、中性胎 18 圈、硬胎 25 圈等），计算当前轮胎寿命偏离基准的程度（`Dev_compound_TL`、`TL_ratio_compound`）。直接对应"轮胎是否该换了"这件事。

### 分组统计与偏差

从车手、比赛、轮胎配方三个维度算分组均值，再生成"当前值 - 分组均值"的偏差特征。

偏差特征的价值在于消除个体基准差异。比如同样是轮胎寿命 20 圈，对长距离保胎型车手和对频繁进站型车手含义不同，偏差特征能区分这种差异。

### 5 折交叉目标编码

共 12 组编码：单特征 5 组（Driver、Race、Compound、Stint、Year）、双特征 6 组、三特征 1 组。

防泄露做法：
1. 训练集分 5 折
2. 每折验证集的编码只由其余 4 折的统计量算出
3. 测试集用全量训练集统计量直接映射

加了带权重的全局先验平滑：

```
平滑后均值 = (类别均值 × 样本数 + gm × 平滑系数) / (样本数 + 平滑系数)
```

单特征平滑系数 100，组合特征 30。样本量越小，平滑力度越大，避免低频类别编码出现极端值。`gm = 0.1990` 取自训练集标签均值。

### 风险乘积特征

把目标编码两两、三三相乘，生成 `Risk_CD`、`Risk_CR`、`Risk_CDR`。用乘积放大高风险组合的信号。

### 频率编码

对 Driver、Race、Compound、Stint 计算训练集中的出现频率。稀有车手/赛道的进站规律可能和普遍情况不同，频率编码从样本量维度补充信息，和目标编码互补。

---

## 模型配置

6 个基模型遵循"算法异构 + 配置差异"的原则，最大化多样性。

### 统一训练范式

所有模型都走"5 个种子循环 → 单种子 5 折分层交叉验证 → 种子结果平均"的流程，输出 OOF 预测和测试集预测。

- **分层抽样**：`StratifiedKFold` 保证每折正负比例一致
- **OOF 预测**：验证折完全由未参与训练的折生成，无标签泄露
- **多种子平均**：5 个种子降低初始化和数据划分的随机性
- **资源回收**：每个模型训练完执行 `gc.collect()`，GPU 模型额外 `torch.cuda.empty_cache()`

### 6 个模型一览

| 模型 | 算法 | 定位 | 关键参数 |
|------|------|------|----------|
| lgb_deep | LightGBM | 主力，捕捉复杂模式 | 6000 树，lr 0.012，383 叶，深度 11 |
| lgb_reg | LightGBM | 泛化互补 | 5000 树，lr 0.02，127 叶，深度 7，强正则 |
| xgb_deep | XGBoost | 算法差异化 | 6000 树，lr 0.012，深度 9，双级列采样 |
| xgb_reg | XGBoost | 轻量泛化 | 4000 树，lr 0.02，深度 6，高正则 |
| cat_gpu | CatBoost | 类别特征优化 | 4000 迭代，lr 0.02，深度 8，Ordered Boosting |
| nn_residual | PyTorch | 深度学习补充 | 4 层残差 MLP，512→256→128→1 |

### 各模型详细参数

#### lgb_deep（LightGBM 深度大容量）

| 参数 | 值 |
|------|-----|
| 迭代数 | 6000 |
| 学习率 | 0.012 |
| 叶子节点数 | 383 |
| 最大深度 | 11 |
| 叶子最小样本 | 25 |
| 特征采样率 | 0.35 |
| 样本采样率 | 0.7（每 3 树重采样） |
| L1 / L2 | 1.5 / 2.5 |
| 分裂最小增益 | 0.008 |
| 早停 | 200 轮 |

高叶子数 + 低学习率 + 大迭代，容量足够大；低特征采样率增加随机性，压低大容量模型的过拟合风险。

#### lgb_reg（LightGBM 正则泛化）

| 参数 | 值 |
|------|-----|
| 迭代数 | 5000 |
| 学习率 | 0.02 |
| 叶子节点数 | 127 |
| 最大深度 | 7 |
| 叶子最小样本 | 50 |
| 特征采样率 | 0.5 |
| 样本采样率 | 0.8（每 5 树重采样） |
| L1 / L2 | 2.0 / 3.0 |
| 分裂最小增益 | 0.02 |
| 早停 | 200 轮 |

和 lgb_deep 形成"深度拟合 + 浅层高正则"的互补，避免整个集成偏向过拟合。

#### xgb_deep（XGBoost 深度）

| 参数 | 值 |
|------|-----|
| 迭代数 | 6000 |
| 学习率 | 0.012 |
| 最大深度 | 9 |
| 叶子最小权重和 | 15 |
| 行采样 | 0.7 |
| 列采样（按树 / 按层） | 0.35 / 0.35 |
| L1 / L2 | 1.5 / 2.5 |
| 分裂最小损失下降 | 0.08 |
| 早停 | 300 轮 |

XGBoost 的分裂算法和 LightGBM 本质不同，预测分布有差异，融合后增益明显。两级列采样（按树 + 按层）增强随机性。

#### xgb_reg（XGBoost 浅树正则）

| 参数 | 值 |
|------|-----|
| 迭代数 | 4000 |
| 学习率 | 0.02 |
| 最大深度 | 6 |
| 叶子最小权重和 | 50 |
| 行采样 | 0.8 |
| 列采样（按树 / 按层） | 0.5 / 0.5 |
| L1 / L2 | 2.0 / 3.0 |
| 分裂最小损失下降 | 0.2 |
| 早停 | 200 轮 |

训练最快、资源占用最低，和 lgb_reg 一起拉高集成的泛化底线。

#### cat_gpu（CatBoost）

| 参数 | 值 |
|------|-----|
| 核心算法 | Ordered Boosting |
| 树生长策略 | Lossguide |
| 迭代数 | 4000 |
| 学习率 | 0.02 |
| 树深度 | 8 |
| 叶子最小样本 | 30 |
| L2 正则 | 5 |
| 边界分箱数 | 128 |
| 随机强度 | 0.5 |
| 贝叶斯采样温度 | 0.5 |
| 早停 | 200 轮，保留最优模型 |

原生支持类别特征，Ordered Boosting 从原理上减少目标泄露，对高基数类别场景特别合适。分裂算法和另外两类树模型差异大，是集成多样性的重要来源。

#### nn_residual（PyTorch 残差网络）

网络结构：

| 层 | 维度 | 组件 |
|----|------|------|
| 输入投影 | input → 512 | Linear + BN + ReLU + Dropout(0.2) |
| 高维残差组 | 512 → 512 | 2 个残差块（每个 2 层 FC + BN + ReLU + Dropout(0.3)） |
| 下采样 | 512 → 256 | Linear + BN + ReLU + Dropout(0.3) |
| 低维残差组 | 256 → 256 | 2 个残差块 |
| 输出 | 256 → 128 → 1 | Linear + BN + ReLU + Dropout(0.3) + Linear + Sigmoid |

训练配置：

| 配置 | 值 |
|------|-----|
| 训练批次 | 1024 |
| 验证批次 | 2048 |
| 损失 | BCELoss |
| 优化器 | AdamW |
| 学习率 | 0.0015 |
| 权重衰减 | 1e-5 |
| 学习率调度 | ReduceLROnPlateau，5 轮不降则减半，最低 1e-6 |
| 最大 epoch | 100 |
| 早停 | 15 轮验证 AUC 无提升 |
| 标准化 | 折内 StandardScaler（只用训练折拟合） |

NN 单模 AUC 通常比树模型低 0.005 左右，但加进集成后仍有 0.001+ 的提升，所以保留。残差结构解决深层梯度消失，BatchNorm 加速收敛，多层 Dropout 防过拟合。和树模型错误模式差异最大，是集成增益的来源。

---

## 集成策略

集成模块采用"线性权重优化 + 二层堆叠 + 最终融合"三级结构。

### 模型相关性分析

算 6 个模型 OOF 预测两两的皮尔逊相关系数。相关性越低，模型错误模式差异越大，融合增益越高。如果所有模型相关性都极高，说明同质化严重，继续集成收益有限。

### 第一层：L-BFGS-B 权重优化

用 SciPy 的 L-BFGS-B 做有约束优化：

- 目标：最小化负验证 AUC（等价于最大化 AUC）
- 约束：权重非负，和为 1
- 20 次随机初始值迭代，每次从狄利克雷分布采样，避免局部最优

相比简单平均能自动给效果好、差异大的模型更高权重；相比网格搜索收敛快、稳定。

### 第二层：5 折交叉堆叠

把 6 个模型的 OOF 预测作为新特征（6 列），5 折分层交叉训练元模型：

| 元模型 | 参数 | 定位 |
|--------|------|------|
| Ridge | alpha=100 | 中等正则线性 |
| Ridge | alpha=500 | 强正则线性 |
| LogisticRegression | C=0.1 | 带 sigmoid 的非线性 |

严格折内训练，元模型完全看不到验证折标签。三个元模型并行对比，取验证 AUC 最高的作为堆叠输出。

> 试过用 LightGBM 做元模型，但线上线下分数差变大，最终退回 Ridge。

### 最终融合

把"权重优化结果"和"最优堆叠结果"等权平均。两种方法原理不同、错误模式不同，简单平均能平滑波动，降低线上线下分数差异的风险。

---

## 输出文件

运行结束后当前目录生成 4 类文件：

| 文件 | 格式 | 说明 |
|------|------|------|
| `submission_v8_gpu.csv` | CSV | 最终提交文件，格式与官方样例一致 |
| `experiment_v8_gpu.json` | JSON | 实验记录（各单模 AUC、集成分数、特征数、耗时） |
| `oof_*.npy` | numpy | 6 个模型的 OOF 验证预测，用于后续集成实验 |
| `test_*.npy` | numpy | 6 个模型的测试集预测，可直接加载做二次融合 |

预测值最终裁剪到 `[0.001, 0.999]`，避免极端概率影响 AUC 排序。控制台会打印完整结果报表（单模 AUC、集成 AUC、最终方法、总耗时）。

`experiment_v8_gpu.json` 字段：

| 字段 | 说明 |
|------|------|
| models | 各单模型验证 AUC |
| optimized_auc | 权重优化后集成 AUC |
| ridge_auc | 最优堆叠 AUC |
| final_auc | 最终融合 AUC |
| method | 最终采用的融合方法 |
| n_features | 使用特征数 |
| elapsed_s | 总耗时（秒） |

---

## 模型效果

> 请在此填写实际成绩。

| 模型 | OOF AUC | 备注 |
|------|---------|------|
| lgb_deep | — | |
| lgb_reg | — | |
| xgb_deep | — | |
| xgb_reg | — | |
| cat_gpu | — | |
| nn_residual | — | |
| 权重优化集成 | — | |
| 堆叠集成 | — | |
| **最终融合** | — | |
| Kaggle 线上 | — | |

---

## 常见问题

### 运行报错

**Q: `FileNotFoundError: No such file or directory: 'train.csv'`**
数据路径不对。把数据放进 `playground-series-s6e5/` 目录，或修改代码中的路径。

**Q: `ValueError: Expected 2D array, got 1D array instead`**
堆叠部分的循环变量名和全局 `oof_stack` 重名，二维矩阵被一维数组覆盖。检查变量命名。

**Q: GPU 没识别，跑在 CPU 上**
CUDA 环境没配好，或装的是 CPU 版 PyTorch。装对应 CUDA 版本的 PyTorch，确认 `torch.cuda.is_available()` 返回 True。LightGBM 4.0+ 的 GPU 参数从 `device='gpu'` 改成了 `device='cuda'`，注意版本差异。

### 效果调优

**Q: 线下 AUC 高，线上分数低**
大概率是数据泄露或过拟合。检查目标编码、时序特征有没有泄露，验证折划分是否合理，适当加大正则。

**Q: 模型间相关性都很高，集成增益小**
增加差异化模型，拉开参数差异，或新增特征工程维度引入新信息源。

**Q: 神经网络明显弱于树模型**
正常。表格数据树模型通常是基线最优，NN 的价值在提供差异化预测，不是单模精度。

---

## 调参方向

### 全局超参

| 参数 | 默认 | 调整建议 |
|------|------|---------|
| SEEDS | 5 个种子 | 增加种子提升稳定性，训练时间线性增加；金牌区建议 5-10 个 |
| NF | 5 | 10 折精度更高但耗时翻倍；数据量小时可提升折数 |
| gm | 0.1990 | 可替换为训练集标签真实均值，平滑系数同步调整 |

### 树模型

| 参数 | 规律 |
|------|------|
| 学习率 | 越低需要越多迭代，泛化通常越好；建议 0.008-0.03 |
| 叶子数 / 深度 | 越大拟合越强越易过拟合，需配合正则 |
| 采样率 | 越低随机性越强，泛化可能提升但波动增大 |
| L1/L2 | 过拟合时加大，欠拟合时减小 |
| 早停轮数 | 太小提前停止，太大浪费时间，需平衡 |

### 神经网络

| 参数 | 规律 |
|------|------|
| 隐藏层维度 | 显存够可加大，需同步加大 Dropout |
| Dropout | 过拟合加大，欠拟合减小 |
| 学习率 | 建议 0.001-0.003，配合调度器 |
| 批次大小 | 越大越稳，GPU 利用率越高；显存不足调小 |
| 早停耐心 | 10-20，太小易早停，太大易过拟合 |

### 集成

- 权重优化迭代次数：默认 20 次够用，可加到 50 次进一步寻优，收益边际递减
- 元模型：可加 SVM、浅层 LightGBM 等
- 最终融合权重：默认等权，可按线下验证调整比例

